# Exercícios 1 ao 4 - TDD com Pytest

## Como executar

1. Abra a pasta no VS Code
2. Crie o ambiente virtual:

Windows:
python -m venv venv

3. Ative o ambiente virtual:

Windows:
venv\Scripts\activate

4. Instale o pytest:
pip install -r requirements.txt

5. Execute os testes:
pytest -v
